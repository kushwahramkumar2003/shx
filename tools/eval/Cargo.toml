# shx-eval: deterministic T6 scoring harness (docs/08-TESTING.md §7).
# In-process only: it translates intents, it never executes a command (ADR-002).
[package]
name = "shx-eval"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic eval harness scoring translate.json fixtures"
repository.workspace = true
# Dev-only harness (like xtask): never published, never released.
publish = false

[[bin]]
name = "shx-eval"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
shx-core.workspace = true
shx-llm.workspace = true
shx-memory.workspace = true
serde.workspace = true
serde_json.workspace = true
clap.workspace = true
thiserror.workspace = true
